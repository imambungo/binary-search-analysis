public class BinaryWorm {
    void binaryWorm(int totalElement) {
        boolean[] child = new boolean[totalElement];
        int m = kiri + (kanan - kiri) / 2;
        BinaryList wormList = new BinaryList();
        wormList.inject(totalElement - 1);
    }

    
}